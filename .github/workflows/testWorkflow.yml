name: TestWorkflow
on: [push]

jobs:
    printTest:
        runs-on: ubuntu-latest
        steps:
            - shell: bash
              env:
                SECRET: ${{ secrets.SECRET }}
              run:
                echo "$SECRET"
